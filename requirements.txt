# ============================================================
# requirements.txt — зафиксированные версии зависимостей
# Установка: pip install -r requirements.txt
# ============================================================

# Django framework
Django==5.1.8

# Загрузка переменных окружения из .env
python-dotenv==1.1.0

# Обработка изображений (ImageField)
Pillow==11.2.1

# WSGI-сервер (нужен на проде)
gunicorn==22.0.0

# PostgreSQL-адаптер (нужен только на проде; для SQLite не требуется)
# Раскомментируйте при деплое на сервер с PostgreSQL:
# psycopg2-binary==2.9.10  ← для быстрого старта
# psycopg2==2.9.10          ← рекомендуется для продакшена (сборка из исходников)
